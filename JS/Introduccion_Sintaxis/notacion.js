// Notacion

// . Se usa en los objetos para acceder a los atributos de los objetos
// Un objeto puede tener varios atributos: telefono.length

// ; Delimitar el final de una linea

// [] Se usan para las listas, arreglos o arrays
const arr = [1,2,3,4]
console.log(arr[0])

// () Se usan para funciones
const suma = (a, b) => {
    return a + b
}

console.log(suma(10,5))

// {} Para objetos literales, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 5
}

if (true) {
    
}